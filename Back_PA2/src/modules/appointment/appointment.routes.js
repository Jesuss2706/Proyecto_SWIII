const { Router } = require('express');

const router = Router();

// TODO: migrar aquí AppointmentController y las estrategias de
// exportación (json/csv/html) del appointment-service original.

module.exports = router;
